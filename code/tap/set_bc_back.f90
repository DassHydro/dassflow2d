!        Generated by TAPENADE     (INRIA, Ecuador team)
!  Tapenade 3.16 (develop) - 25 Jun 2025 16:38
!
!  Differentiation of set_bc in reverse (adjoint) mode (with options fixinterface):
!   gradient     of useful results: *(dof.h)
!   with respect to varying inputs: *(dof.h)
!   Plus diff mem management of: dof.h:in mesh.edge:in
SUBROUTINE SET_BC_BACK(dof, dof_back, mesh, mesh_back)
  USE M_NUMERIC ! Replaced by Perl Script
  USE M_NUMERIC_BACK
  USE M_MODEL ! Replaced by Perl Script
  USE M_MPI ! Replaced by Perl Script

  USE M_TAP_VARS ! Added by Perl Script -> Need to be filled !!!

  IMPLICIT NONE
  TYPE(MSH), INTENT(IN) :: mesh
  TYPE(MSH) :: mesh_back ! Replaced by Perl Script
  TYPE(UNK), INTENT(INOUT) :: dof
  TYPE(UNK), INTENT(INOUT) :: dof_back
  REAL(rp) :: sum_pow_h, zin, qin, qout
  INTEGER(ip) :: num_bc, up_num_bc, down_num_bc, connected_num_bc
  INTRINSIC SIZE
  INTRINSIC MAX
  REAL(rp) :: y1
  REAL(rp) :: y2
  REAL(rp) :: max1
  REAL(rp) :: tmp
  REAL(rp) :: tmp0
  INTEGER*4 :: branch
  DO num_bc=1,bc%nb
    IF (bc%typ(num_bc, 1) .EQ. 'discharg1') THEN
      DO ib=1,mesh%neb
        IF (mesh%edgeb(ib)%typlim .EQ. 'discharg1' .AND. mesh%edgeb(ib)%&
&           group .EQ. num_bc) THEN
          ie = mesh%edgeb(ib)%ind
          i = mesh%edge(ie)%cell(1)
!To review before merge
          IF (dof%h(i) .LE. heps) THEN
            CALL PUSHREAL8(dof%h(i))
            dof%h(i) = 0.0001
            CALL PUSHCONTROL2B(1)
          ELSE
            CALL PUSHCONTROL2B(2)
          END IF
        ELSE
          CALL PUSHCONTROL2B(0)
        END IF
      END DO
      CALL PUSHCONTROL1B(0)
      CALL MPI_SUM_R(sum_pow_h)
    ELSE
      CALL PUSHCONTROL1B(1)
    END IF
    CALL PUSHCONTROL1B(0)
    CALL PUSHCONTROL1B(0)
    CALL MPI_SUM_R(qout)
    CALL PUSHCONTROL1B(1)
    CALL MPI_SUM_R(sum_pow_h)
    CALL PUSHCONTROL1B(1)
  END DO
  DO num_bc=bc%nb,1,-1
    CALL POPCONTROL1B(branch)
    IF (branch .NE. 0) CALL POPCONTROL1B(branch)
    CALL POPCONTROL1B(branch)
    IF (branch .EQ. 0) THEN
      DO ib=mesh%neb,1,-1
        CALL POPCONTROL2B(branch)
        IF (branch .NE. 0) THEN
          IF (branch .EQ. 1) THEN
            ie = mesh%edgeb(ib)%ind
            i = mesh%edge(ie)%cell(1)
            CALL POPREAL8(dof%h(i))
            dof_back%h(i) = 0.0_8
          END IF
        END IF
      END DO
    END IF
  END DO
END SUBROUTINE SET_BC_BACK

